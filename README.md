# Quick maths

## Running quick check

Run it via `cargo test`, you can control the number of tests `quick-check` generates
by chaning the following env variable `QUICKCHECK_TESTS=x`, so you can do
`QUICKCHECK_TESTS=10000 cargo test`
